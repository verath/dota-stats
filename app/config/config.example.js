module.exports = {
    // http://steamcommunity.com/dev/apikey
    'STEAM_API_KEY': '',

    // This is used by the OpenId sign-in as the redirect url.
    SITE_URL: 'http://127.0.0.1:3000/',

    // null means use the default options, most likely what you want
    'REDIS_PORT': null,
    'REDIS_HOST': null,
    'REDIS_OPTIONS': null
};